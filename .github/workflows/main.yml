on:  
  push:  
    branches:  
      - master  

jobs:  
  build:  
    runs-on: ubuntu-latest  

    steps:  
      - uses: actions/checkout@v3  
      
      - name: "login to docker hub"  
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login --username youssefadl --password-stdin
